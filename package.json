{
  "name": "ai-interview-chatbot",
  "version": "0.1.0",
  "private": true,
  "description": "AI interview practice web app prototype",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check ai-interview-prototype/script.js && node --check ai-interview-prototype/firebase-cloud.js && node --check ai-interview-prototype/firebase-config.js && node --check server.mjs",
    "test": "node --test \"test/**/*.test.js\"",
    "firebase:deploy:rules": "firebase deploy --only firestore:rules"
  },
  "engines": {
    "node": ">=18"
  }
}
